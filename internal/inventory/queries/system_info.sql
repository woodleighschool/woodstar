SELECT
    hostname,
    computer_name,
    uuid,
    hardware_serial,
    hardware_model,
    hardware_version,
    hardware_vendor,
    cpu_type,
    cpu_subtype,
    cpu_brand,
    cpu_logical_cores,
    cpu_physical_cores,
    physical_memory
FROM system_info;
