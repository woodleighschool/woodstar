// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: sessions.sql

package sqlc

import (
	"context"
	"time"
)

const createSession = `-- name: CreateSession :exec
INSERT INTO sessions (
    user_id,
    token_hash,
    expires_at
)
VALUES (
    $1,
    $2,
    $3
)
`

type CreateSessionParams struct {
	UserID    int64     `json:"user_id"`
	TokenHash string    `json:"token_hash"`
	ExpiresAt time.Time `json:"expires_at"`
}

func (q *Queries) CreateSession(ctx context.Context, arg CreateSessionParams) error {
	_, err := q.db.Exec(ctx, createSession, arg.UserID, arg.TokenHash, arg.ExpiresAt)
	return err
}

const getUserByActiveSessionToken = `-- name: GetUserByActiveSessionToken :one
WITH refreshed_session AS (
    UPDATE sessions
    SET last_seen_at = $1
    WHERE token_hash = $2
      AND revoked_at IS NULL
      AND expires_at > $1
    RETURNING user_id
)
SELECT
    users.id,
    users.email,
    users.name,
    users.password_hash,
    users.role,
    users.created_at,
    users.updated_at,
    users.deleted_at
FROM users
JOIN refreshed_session ON refreshed_session.user_id = users.id
WHERE users.deleted_at IS NULL
`

type GetUserByActiveSessionTokenParams struct {
	SeenAt    *time.Time `json:"seen_at"`
	TokenHash string     `json:"token_hash"`
}

func (q *Queries) GetUserByActiveSessionToken(ctx context.Context, arg GetUserByActiveSessionTokenParams) (User, error) {
	row := q.db.QueryRow(ctx, getUserByActiveSessionToken, arg.SeenAt, arg.TokenHash)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.Name,
		&i.PasswordHash,
		&i.Role,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.DeletedAt,
	)
	return i, err
}

const revokeSession = `-- name: RevokeSession :exec
UPDATE sessions
SET revoked_at = now()
WHERE token_hash = $1 AND revoked_at IS NULL
`

type RevokeSessionParams struct {
	TokenHash string `json:"token_hash"`
}

func (q *Queries) RevokeSession(ctx context.Context, arg RevokeSessionParams) error {
	_, err := q.db.Exec(ctx, revokeSession, arg.TokenHash)
	return err
}

const revokeSessionsForUser = `-- name: RevokeSessionsForUser :exec
UPDATE sessions
SET revoked_at = now()
WHERE user_id = $1 AND revoked_at IS NULL
`

type RevokeSessionsForUserParams struct {
	UserID int64 `json:"user_id"`
}

func (q *Queries) RevokeSessionsForUser(ctx context.Context, arg RevokeSessionsForUserParams) error {
	_, err := q.db.Exec(ctx, revokeSessionsForUser, arg.UserID)
	return err
}
