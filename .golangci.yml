---
version: "2"

issues:
  max-same-issues: 0

formatters:
  enable:
    - goimports
    - golines
    - gci
    - gofumpt
  settings:
    goimports:
      local-prefixes:
        - github.com/woodleighschool/woodstar
    gci:
      sections:
        - standard
        - default
        - prefix(github.com/woodleighschool/woodstar)
      custom-order: true
    golines:
      max-len: 120

linters:
  default: none
  enable:
    - bodyclose
    - copyloopvar
    - errcheck
    - errname
    - errorlint
    - exhaustive
    - exptostd
    - fatcontext
    - gocritic
    - govet
    - ineffassign
    - intrange
    - makezero
    - mirror
    - misspell
    - modernize
    - nilerr
    - nilnesserr
    - nilnil
    - noctx
    - perfsprint
    - revive
    - rowserrcheck
    - sqlclosecheck
    - staticcheck
    - testifylint
    - unconvert
    - unused
    - usestdlibvars
    - usetesting

  settings:
    errcheck:
      check-type-assertions: true

    exhaustive:
      check:
        - switch
        - map
      default-signifies-exhaustive: true

    gocritic:
      disabled-checks:
        - ifElseChain
        - singleCaseSwitch

    govet:
      enable:
        - nilness

    perfsprint:
      strconcat: false

    revive:
      severity: warning
      rules:
        - name: dot-imports
        - name: error-return
        - name: error-strings
        - name: errorf
        - name: indent-error-flow
        - name: range
        - name: receiver-naming
        - name: redefines-builtin-id
        - name: superfluous-else
        - name: time-naming
        - name: unreachable-code
        - name: var-declaration

    staticcheck:
      checks:
        - all
        - -ST1000  # missing package comment
        - -ST1003  # poorly chosen identifier
        - -ST1016  # inconsistent receiver name
        - -ST1020  # exported func comment
        - -ST1021  # exported type comment
        - -ST1022  # exported var/const comment
        - -QF1008  # omit embedded fields from selector

    usetesting:
      os-temp-dir: true

  exclusions:
    generated: lax
    warn-unused: true
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      - path: _test\.go
        linters:
          - bodyclose
          - errcheck
          - noctx

run:
  relative-path-mode: gomod
